package dragonsaga.inventory.container;

import necesse.engine.network.NetworkClient;
import necesse.inventory.container.Container;

public class DragonRaceContainer extends Container {
    public DragonRaceContainer(NetworkClient client, int uniqueSeed) {
        super(client, uniqueSeed);
    }



}
